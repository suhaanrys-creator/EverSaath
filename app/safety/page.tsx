'use client'; import { useState } from 'react'; import { AlertTriangle, Check, MapPin, PhoneCall, ShieldCheck, UserRound } from 'lucide-react'; import PageHeader from '@/components/PageHeader'; import Modal from '@/components/Modal';
export default function Safety(){const [sos,setSos]=useState(false);return <main><PageHeader eyebrow="Here when it matters" title="Safety should feel simple." body="Clear options, trusted people and support that respects your choices. Explore this demo of the EverSaath safety flow."/><section className="section-pad"><div className="container grid gap-7 lg:grid-cols-[.8fr_1.2fr]"><div className="rounded-[28px] bg-[#fff2ce] p-8 text-center"><AlertTriangle size={38} className="mx-auto text-[#b87900]"/><h2 className="mt-5 text-2xl font-extrabold text-navy">SOS demo</h2><p className="mt-3 text-[#6e5d2a]">This button opens a confirmation only. It does not contact emergency services.</p><button onClick={()=>setSos(true)} className="mt-7 flex w-full items-center justify-center gap-2 rounded-full bg-[#c44c3d] px-6 py-5 text-lg font-extrabold text-white shadow-lg hover:bg-[#a83e32]"><PhoneCall/> I need urgent help</button><button className="mt-3 w-full rounded-full border-2 border-navy px-6 py-3 font-extrabold text-navy">I need non-emergency help</button></div><div className="grid gap-5 md:grid-cols-2">{[[ShieldCheck,'Trust & verification','Background checks, verified profiles, ratings and a clear support process.'],[MapPin,'Consent-based location','Location sharing is always a choice, visible to you and easy to turn off.'],[UserRound,'Your emergency contacts','Choose the family members or friends you want to include in your support circle.'],[Check,'Clear expectations','We do not promise a fixed emergency response time. Call local emergency services for immediate danger.']].map(([I,title,body])=>{const Icon=I as typeof ShieldCheck;return <div key={title as string} className="rounded-[20px] border border-[#e3eee8] bg-white p-6 shadow-soft"><Icon className="text-teal"/><h2 className="mt-4 font-extrabold text-navy">{title as string}</h2><p className="mt-2 text-sm text-[#657888]">{body as string}</p></div>})}</div></div></section><Modal open={sos} onClose={()=>setSos(false)} title="Please confirm you need help"><p>This MVP demo would show your chosen contacts and next steps here. For immediate danger, call your local emergency number now.</p></Modal></main>}
