'use client'; import { useState } from 'react'; import PageHeader from '@/components/PageHeader'; import CompanionCard from '@/components/CompanionCard'; import { companions } from '@/data/mock';
export default function Companions(){const [query,setQuery]=useState('');const shown=companions.filter(x=>`${x.name} ${x.languages} ${x.interests} ${x.location}`.toLowerCase().includes(query.toLowerCase()));return <main><PageHeader eyebrow="Find your people" title="Good company, on your terms." body="Meet warm, verified companions who share your interests and make everyday moments brighter."/><section className="section-pad"><div className="container"><div className="mb-10 grid gap-3 rounded-[22px] bg-sage p-5 md:grid-cols-4"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search language, interest or area" className="rounded-xl border-0 bg-white px-4 py-3 text-navy outline-teal md:col-span-2"/><select className="rounded-xl border-0 bg-white px-4 py-3 text-navy"><option>Any availability</option><option>Weekday mornings</option><option>Evenings</option><option>Weekends</option></select><select className="rounded-xl border-0 bg-white px-4 py-3 text-navy"><option>Any kind of help</option><option>Walking</option><option>Technology</option><option>Conversation</option></select></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{shown.map(x=><CompanionCard key={x.name} companion={x}/>)}</div>{!shown.length&&<p className="py-12 text-center font-bold text-navy">No companions match that search yet.</p>}</div></section></main>}
