'use client'; import { useState } from 'react'; import PageHeader from '@/components/PageHeader'; import ServiceCard from '@/components/ServiceCard'; import { services } from '@/data/mock';
export default function Services(){const [filter,setFilter]=useState('All');const categories=['All','Companionship','Healthcare','Care','Everyday Help','Community'];const shown=filter==='All'?services:services.filter(x=>x.category===filter);return <main><PageHeader eyebrow="Support that fits" title="A little help can make a big difference." body="Choose the kind of support that helps you keep doing the things you love."/><section className="section-pad"><div className="container"><div className="mb-10 flex flex-wrap gap-2">{categories.map(x=><button key={x} onClick={()=>setFilter(x)} className={`rounded-full px-5 py-3 text-sm font-extrabold ${filter===x?'bg-navy text-white':'bg-sage text-navy hover:bg-[#d7eadb]'}`}>{x}</button>)}</div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{shown.map(service=><ServiceCard key={service.title} service={service}/>)}</div><p className="mt-10 rounded-2xl bg-[#fff2ce] p-5 text-sm font-bold text-[#785800]">EverSaath connects members with support services. It does not replace emergency medical services or professional medical advice.</p></div></section></main>}
