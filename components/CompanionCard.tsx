'use client'; import Image from 'next/image'; import { MapPin, Star, BadgeCheck } from 'lucide-react'; import { useState } from 'react'; import Modal from './Modal'; import type { Companion } from '@/data/mock';
export default function CompanionCard({companion}:{companion:Companion}){const [open,setOpen]=useState(false);return <><article className="overflow-hidden rounded-[22px] border border-[#e3eee8] bg-white shadow-soft"><div className="relative h-56"><Image src={companion.image} alt={`${companion.name}, companion`} fill className="object-cover"/><span className="absolute bottom-3 left-3 flex items-center gap-1 rounded-full bg-white px-3 py-1 text-xs font-extrabold text-emerald"><BadgeCheck size={14}/> Verified</span></div><div className="p-5"><div className="flex justify-between"><div><h2 className="text-xl font-extrabold text-navy">{companion.name}, {companion.age}</h2><p className="mt-1 flex items-center gap-1 text-sm text-[#657888]"><MapPin size={14}/>{companion.location}</p></div><span className="flex items-center gap-1 font-extrabold text-[#9a6900]"><Star size={15} fill="currentColor"/>{companion.rating}</span></div><p className="mt-4 text-sm font-bold text-navy">{companion.languages}</p><p className="mt-1 text-sm text-[#657888]">Loves {companion.interests.toLowerCase()}.</p><div className="mt-5 flex items-center justify-between"><span className="font-extrabold text-teal">{companion.price}</span><button onClick={()=>setOpen(true)} className="rounded-full bg-navy px-4 py-2 text-sm font-extrabold text-white hover:bg-teal">Book companion</button></div></div></article><Modal open={open} onClose={()=>setOpen(false)} title={`Meet ${companion.name}`}><p>We will help you request a conversation with {companion.name}. This mock booking does not process payment.</p></Modal></>}
