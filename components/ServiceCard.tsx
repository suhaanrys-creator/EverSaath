'use client'; import * as Icons from 'lucide-react'; import { useState } from 'react'; import Modal from './Modal'; import type { Service } from '@/data/mock';
export default function ServiceCard({service}:{service:Service}){const [open,setOpen]=useState(false); const Icon=(Icons as unknown as Record<string,React.ElementType>)[service.icon]||Icons.HeartHandshake; return <><article className="flex flex-col rounded-[22px] border border-[#e3eee8] bg-white p-6 shadow-soft transition hover:-translate-y-1"><div className="flex items-start justify-between"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-sage text-teal"><Icon size={24}/></div><span className="rounded-full bg-[#fff2ce] px-3 py-1 text-xs font-extrabold text-[#9a6900]">{service.category}</span></div><h2 className="mt-6 text-xl font-extrabold text-navy">{service.title}</h2><p className="mt-2 flex-1 text-[#647789]">{service.description}</p><div className="mt-6 flex items-center justify-between border-t border-sage pt-4 text-sm font-bold text-[#657888]"><span>{service.price}</span><span>{service.duration}</span></div><button onClick={()=>setOpen(true)} className="mt-5 w-full rounded-full bg-navy px-4 py-3 font-extrabold text-white hover:bg-teal">Book now</button></article><Modal open={open} onClose={()=>setOpen(false)} title={`Request ${service.title}`}><p>We have noted your interest. An EverSaath coordinator will contact you with available options. This is a mock booking for the MVP.</p></Modal></>}
