export default function SectionHeading({eyebrow,title,body,center=false}:{eyebrow:string;title:string;body?:string;center?:boolean}){return <div className={`max-w-2xl ${center?'mx-auto text-center':''}`}><p className="eyebrow mb-3">{eyebrow}</p><h2 className="text-3xl font-extrabold leading-tight text-navy md:text-5xl">{title}</h2>{body&&<p className="mt-4 text-lg text-[#5d7181]">{body}</p>}</div>}
