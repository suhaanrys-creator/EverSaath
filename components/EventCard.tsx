'use client'; import * as Icons from 'lucide-react'; import { useState } from 'react'; import Modal from './Modal'; import type { EventItem } from '@/data/mock';
export default function EventCard({event}:{event:EventItem}){const [open,setOpen]=useState(false);const Icon=(Icons as unknown as Record<string,React.ElementType>)[event.icon]||Icons.Sparkles;return <article className="rounded-[22px] border border-[#e3eee8] bg-white p-6 shadow-soft"><div className="flex items-start gap-4"><div className="min-w-[68px] rounded-2xl bg-navy p-3 text-center text-white"><p className="text-lg font-extrabold leading-none">{event.date.split(' ')[0]}</p><p className="mt-1 text-xs uppercase">{event.date.split(' ')[1]}</p></div><div><div className="flex items-center gap-2 text-teal"><Icon size={19}/><span className="text-xs font-extrabold uppercase tracking-wider">{event.level}</span></div><h2 className="mt-2 text-xl font-extrabold text-navy">{event.title}</h2></div></div><div className="mt-5 space-y-1 text-sm text-[#657888]"><p>{event.time}</p><p>{event.place}</p></div><button onClick={()=>setOpen(true)} className="mt-5 w-full rounded-full border-2 border-navy px-4 py-2 font-extrabold text-navy hover:bg-navy hover:text-white">Join event</button><Modal open={open} onClose={()=>setOpen(false)} title="You’re on the list"><p>We’ll save your interest in {event.title} and share the next details with you. This is a mock RSVP.</p></Modal></article>}
